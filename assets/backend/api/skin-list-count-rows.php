<?php
// กำหนดจำนวนแถวที่ต้องการแสดงต่อหน้า
$rowsPerPage = 10;

// ตรวจสอบค่าพารามิเตอร์ของหน้า (Page) หากไม่มีจะใช้หน้าแรก
$page = isset($_GET['page']) ? (int)$_GET['page'] : 1;

// คำนวณค่าเริ่มต้นของแถวที่จะแสดงในหน้า
$startRow = ($page - 1) * $rowsPerPage;
?>